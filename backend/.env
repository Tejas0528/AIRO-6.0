MONGODB_URI=mongodb://127.0.0.1:27017/airo6
JWT_SECRET=change_this_to_a_long_random_string
QR_SIGNING_SECRET=change_this_to_a_different_long_random_string
PORT=5000
CLIENT_URL=http://localhost:5173

# The procedural Three.js robots do NOT require any external 3D-generation
# API. These are left here only so the app never accidentally expects them.
3D_PROVIDER=none
TRIPO_API_KEY=
MESHY_API_KEY=
